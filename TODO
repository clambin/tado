Won't do:

/homes/<home>/zones/<zone>/details: same as GetZone(id)

Can't do:

/homes/<home>/zones/<zone>/openWindowDetection: return 403 Forbidden?
/homes/<home>/zones/<zone>/dazzle: return 403 Forbidden?

/homes/<home>/incidents (minder): no data
/homes/<home>installations: no data
/homes/<home>/invitations: no data
