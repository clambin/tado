APIs to add:

/homes/<home>/incidents (minder)
/homes/<home>installations
/homes/<home>/zones/<zone>/dayReport?date=%s
/homes/<home>/invitations
/homes/<home>/zones/<zone>/dazzle

Won't do:

/homes/<home>/zones/<zone>/details: same as GetZone(id)

Can't do:

/homes/<home>/zones/<zone>/openWindowDetection: return 403 Forbidden?
